import { handleRequest, handleAsync } from './Functions/AsyncHandler'
import { default as StorageUtils } from './Functions/BrowserStorage'
import { capitalize } from 'Utils/Functions/capitalize'

export {
  handleRequest,
  handleAsync,
  StorageUtils,
  capitalize,
}
